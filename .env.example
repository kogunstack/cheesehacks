# Optional: required only for real auth. Without these, the app still runs locally (you’ll see the login page; sign-in won’t work).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
